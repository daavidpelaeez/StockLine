<Window x:Class="WpfApp1.Views.ProveedoresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="ProveedoresWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- Estilo para botones principales -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004B80"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Formulario -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Botones inferiores -->
        </Grid.RowDefinitions>

        <!-- Formulario superior -->
        <GroupBox Header="Datos del Proveedor" FontWeight="Bold" FontSize="14" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="Nombre:" VerticalAlignment="Center" Grid.Column="0"/>
                <TextBox x:Name="txtNombre" Grid.Column="1" Margin="5"/>

                <Label Content="Contacto:" VerticalAlignment="Center" Grid.Column="2"/>
                <TextBox x:Name="txtContacto" Grid.Column="3" Margin="5"/>
            </Grid>
        </GroupBox>

        <!-- DataGrid central -->
        <DataGrid x:Name="dataGridProveedores" Grid.Row="1" Margin="0,10,0,10"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionMode="Single" AlternatingRowBackground="#e8f0fe"
                  GridLinesVisibility="None" RowBackground="White"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                <DataGridTextColumn Header="Contacto" Binding="{Binding Contacto}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botones inferiores -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button x:Name="btnAgregar" Width="100" Content="Agregar" Click="btnAgregar_Click"/>
            <Button x:Name="btnEditar" Width="100" Content="Editar" Click="btnEditar_Click"/>
            <Button x:Name="btnEliminar"  Width="100" Content="Eliminar" Click="btnEliminar_Click"/>
        </StackPanel>
        <Button x:Name="btnInicio"  Width="100" Content="Inicio" Click="btnInicio_Click" HorizontalAlignment="Left" Grid.Row="2"/>
    </Grid>
</Window>

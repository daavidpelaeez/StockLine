<Window x:Class="WpfApp1.Views.AjustesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="AjustesWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- Estilo general de botón -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E5EA"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D1D1D6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para títulos de secciones -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Ajustes"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Contenido -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Background="Transparent" Margin="0,0,0,20">

                <!-- Cuenta -->
                <Border Background="White" Padding="20" CornerRadius="10" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Cuenta" Style="{StaticResource SectionTitle}"/>
                        <StackPanel Orientation="Vertical" Margin="0,10,0,0" >
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="Nombre de usuario" Width="200" VerticalAlignment="Center"/>
                                <TextBox Width="300" Text="Admin" BorderBrush="#DDD" BorderThickness="1" Height="30"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="Correo electrónico" Width="200" VerticalAlignment="Center"/>
                                <TextBox Width="300" Text="admin@empresa.com" BorderBrush="#DDD" BorderThickness="1" Height="30"/>
                            </StackPanel>
                            <Button Content="Cambiar contraseña" Width="200" Margin="0,10,0,0" Background="#4A90E2" Foreground="White"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Aplicación -->
                <Border Background="White" Padding="20" CornerRadius="10" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Aplicación" Style="{StaticResource SectionTitle}"/>
                        <StackPanel Orientation="Vertical" Margin="0,10,0,0" >
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="Tema oscuro" Width="200" VerticalAlignment="Center"/>
                                <CheckBox Content="Activar" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Idioma" Width="200" VerticalAlignment="Center"/>
                                <ComboBox Width="200" SelectedIndex="0">
                                    <ComboBoxItem Content="Español"/>
                                    <ComboBoxItem Content="Inglés"/>
                                    <ComboBoxItem Content="Francés"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Notificaciones -->
                <Border Background="White" Padding="20" CornerRadius="10">
                    <StackPanel>
                        <TextBlock Text="Notificaciones" Style="{StaticResource SectionTitle}"/>
                        <StackPanel Orientation="Vertical" Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="Notificar nuevos envíos" Width="220" VerticalAlignment="Center"/>
                                <CheckBox IsChecked="True" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="Notificar pedidos recibidos" Width="220" VerticalAlignment="Center"/>
                                <CheckBox IsChecked="True" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="Notificar errores del sistema" Width="220" VerticalAlignment="Center"/>
                                <CheckBox VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Botón Guardar -->
                <Button Content="Guardar cambios"
                        Width="180"
                        Height="42"
                        Background="#4A90E2"
                        Foreground="White"
                        Margin="0,25,0,0"
                        HorizontalAlignment="Right"
                        FontWeight="Bold"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

